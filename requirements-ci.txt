# CI / mock path only. MJD_PROFILE=ci runs all-mock adapters which are pure
# stdlib, so this installs only the light core plus the test/lint toolchain.
# No torch, no cloud SDKs, no Postgres driver. Pinned to latest versions.

pydantic==2.13.4
pyyaml==6.0.3
numpy==2.5.0
fastapi==0.138.1
uvicorn==0.49.0
httpx==0.28.1
python-docx==1.2.0
pypdf==6.14.2
beautifulsoup4==4.15.0

pytest==9.1.1
ruff==0.15.20
black==26.5.1
mypy==2.1.0
