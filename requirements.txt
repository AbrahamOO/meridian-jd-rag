# Full local developer install (zero-key default profile path).
# Pinned to latest versions. Heavy ML and cloud SDKs included here; for the light
# CI/mock path use requirements-ci.txt instead.

# Core (light)
pydantic==2.13.4
pyyaml==6.0.3
numpy==2.5.0
fastapi==0.138.1
uvicorn==0.49.0
python-docx==1.2.0
pypdf==6.14.2
beautifulsoup4==4.15.0

# Local ML stack (brings torch)
sentence-transformers==5.6.0
torch==2.12.1

# Vector store / Postgres
psycopg[binary]==3.3.4
pgvector==0.4.2

# Orchestration
langgraph==1.2.6

# PII redaction
presidio-analyzer==2.2.363
presidio-anonymizer==2.2.363

# Evals
ragas==0.4.3

# Cloud provider SDKs
anthropic==0.112.0
openai==2.44.0
google-genai==2.10.0

# Dev / test / lint
pytest==9.1.1
ruff==0.15.20
black==26.5.1
mypy==2.1.0
